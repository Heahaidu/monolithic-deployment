pipeline {
    agent any 
    environment {
        AWS_REGION = 'us-east-1'
    }
    stages {
        stage('Builds') {
            steps {
                sh '''
                pwd
                '''
            }
        }
    }
}